# APP directory

This directory is **active** and intentionally kept as-is.

## Purpose
- Contains `/APP/index.html`, a maintained reference variant used for:
  - Android Studio loading and comparison workflows
  - i18n clone/reference checks
  - PWA behavior comparison against the canonical root homepage

## Status
- `/APP/index.html` is **not obsolete**.
- The canonical production homepage remains `/index.html` at repository root.
- Historical root-level variants have been isolated under `/archive/`.
